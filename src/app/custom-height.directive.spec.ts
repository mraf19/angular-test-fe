import { CustomHeightDirective } from './custom-height.directive';

describe('CustomHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomHeightDirective();
    expect(directive).toBeTruthy();
  });
});
